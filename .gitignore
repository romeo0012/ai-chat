node_modules/
package-lock.json
.env

# Regenerable data caches (kept out of the repo)
# (docs_cache is now committed so translations ship in the image)
data/page-images.json

# Python cache (FastAPI fallback)
__pycache__/
*.pyc

# Logs
*.log
/tmp/*
